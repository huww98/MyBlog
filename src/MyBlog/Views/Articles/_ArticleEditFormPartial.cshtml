@model Article

@{string actionName = Model == null ? "Create" : "Edit";}
<div class="modal fade" id="imageModal" tabindex="-1" role="dialog" aria-labelledby="imageModalLabel">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="imageModalLabel">插入图片</h4>
            </div>
            <div class="modal-body">
                <!-- Nav tabs -->
                <ul class="nav nav-tabs" role="tablist">
                    <li role="presentation" class="active"><a href="#imageUpload" aria-controls="imageUpload" role="tab" data-toggle="tab">上传图片</a></li>
                    <li role="presentation"><a href="#imageSelect" aria-controls="imageSelect" role="tab" data-toggle="tab" id="imageSelectTab">选择图片</a></li>
                </ul>

                <!-- Tab panes -->
                <div class="tab-content">
                    <div role="tabpanel" class="tab-pane active" id="imageUpload">
                        <input id="imageFile" name="imageFile" type="file" multiple class="file-loading">
                    </div>
                    <div role="tabpanel" class="tab-pane" id="imageSelect">
                        @{ IEnumerable<Image> images = ViewBag.Images; }
                        <div class="img-select-grid">
                            @foreach (var img in images)
                            {
                                <div role="checkbox" class="img-select-item" data-src="@img.Url" data-alt="@img.Alt" data-title="@img.Discription">
                                    <img src="@img.Url" alt="@img.Alt" title="@img.Discription" />
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<form asp-action=@actionName id="article-form" autocomplete="off">
    <div class="form-horizontal">
        <h4>文章</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @if (Model != null)
        {
            <input type="hidden" asp-for="ID" />
        }
        <div class="form-group">
            <label asp-for="Title" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-2 control-label">链接</label>
            <div class="col-md-10">
                <div class="input-group">
                    <span class="input-group-addon">@(Context.Request.Scheme)://@(Context.Request.Host)/</span>
                    <input asp-for="Slug" class="form-control" />
                </div>
                <span asp-validation-for="Slug" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="Categories" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                @(await Component.InvokeAsync<CategorySelectorViewComponent>(new
            {
                Name = "CategoryIDs",
                PreselectedIDs = ViewBag.CategoryIDs,
                IsMultiple = true
            }))
            </div>
        </div>
        <div class="form-group">
            <label asp-for="Content" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <div class="btn-group">
                    <a class="btn btn-default" data-toggle="modal" data-target="#imageModal" title="插入图片"><i class="fa fa-picture-o"></i></a>
                </div>
                <div class="row">
                    <div class="col-sm-6">
                        <textarea asp-for="Content" class="form-control" style="height:500px"></textarea>
                    </div>
                    <div class="col-sm-6">
                        <div id="markdown-html" style="height:500px" class="markdown-preview"></div>
                    </div>
                </div>
                <span class="text-danger" asp-validation-for="Content"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-default">
                    @(Model == null ? "新建" : "保存")
                </button>
            </div>
        </div>
    </div>
</form>